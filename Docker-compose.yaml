services:
  backend:
    build: .
    container_name: backend
    volumes:
      - ./core:/app
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=test
      - DEBUG=True